import styled from "styled-components";
import {theme} from "../../../styles/Theme";

const Link = styled.a`
    font-weight: 700;
    color: ${theme.colors.primaryText};
    white-space: nowrap;
`;

export const Styles = {
    Link
}